import React from 'react';
import PropTypes from 'prop-types';

const Spinner = () => (
    <div className="spinner">
        <div className="img"/>
        <p>&nbsp;&nbsp;&nbsp;loading...</p>
    </div>
);

export default Spinner;
